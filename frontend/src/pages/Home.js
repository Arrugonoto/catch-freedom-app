import React from "react";
import LoginForm from "../components/LoginForm";

const Home = () => {
  return (
    <section className="home-mainpage">
      <LoginForm />
    </section>
  );
};

export default Home;
