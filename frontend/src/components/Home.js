import React from "react";
import LoginForm from "./LoginForm";
import RegisterForm from "./RegisterForm";

const Home = () => {
  return (
    <section className="home-mainpage">
      <LoginForm />
    </section>
  );
};

export default Home;
